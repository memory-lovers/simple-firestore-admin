export const enum WHERE_OP {
  EQ = "=",
  LT = "<",
  GT = ">"
}

export const enum ORDER_TYPE {
  ASC = "ASC",
  DESC = "DESC"
}

export enum FIELD_TYPE {
  STR = "string",
  NUM = "number",
  BOOL = "boolean"
}
